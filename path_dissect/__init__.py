"""
path_dissect — CONCH-Dissect: concept-based dissection of pathology DNNs.

Subpackages
-----------
similarity    — similarity functions (soft_wpmi, wpmi, cos_similarity, etc.)
vlms          — VLM wrappers (CLIPWrapper, PLIPWrapper, CONCHWrapper) + load_vlm factory
probe_models  — target/probe model definitions (ConceptEmbeddingModel / CEM-MIL)
datasets      — dataset loading (TCGA, ImageNet, Broden, CIFAR) + target model loading
utils         — activation extraction, VLM embedding, pipeline orchestration
"""
